
export function addToCart(event) {
    //probably need to input object itself or find by event
    //update card button to slider

    //send request to server
    console.log(event);
}


function fillSmallDishInfo() {

}